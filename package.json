{
  "name": "mcp-server-memrise",
  "version": "0.1.0",
  "description": "Expose the Memrise API as an MCP server",
  "type": "module",
  "bin": {
    "mcp-server-memrise": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "prepublishOnly": "bun run build",
    "check": "tsc --noEmit",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.1",
    "memrise": "^0.4.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3"
  }
}
