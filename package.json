{
  "name": "mcp-server-memrise",
  "version": "0.4.0",
  "description": "MCP server to manage Memrise courses, levels, and words.",
  "type": "module",
  "bin": {
    "mcp-server-memrise": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "prepublishOnly": "bun run build",
    "check": "tsc --noEmit",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.1",
    "memrise": "^0.7.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3"
  }
}
